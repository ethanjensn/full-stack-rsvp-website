{
  "name": "full-stack-rsvp-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "bundle-templates": "node scripts/bundle-templates.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
